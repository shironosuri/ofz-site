import type { BlockMeta } from '../blockMeta'

export const meta = {
  slug: 'code',
  surfaces: ['inline'],
  purpose: 'Displays a syntax-highlighted code sample inside article body text.',
  composesWith: [],
} satisfies BlockMeta
