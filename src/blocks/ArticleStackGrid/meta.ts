import type { BlockMeta } from '../blockMeta'

export const meta = {
  slug: 'articleStackGrid',
  surfaces: ['inline'],
  purpose: 'Displays grouped comparison or reference items in a stacked grid within article body text.',
  composesWith: [],
} satisfies BlockMeta
